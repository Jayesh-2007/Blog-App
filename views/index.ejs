<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>BlogX - Honest Data, Complete Universe</title>
    <link rel="stylesheet" href="/CSS/style.css">
</head>
<body>
    <main class="container">
        <div class="actions">
            <a class="btn" href="/posts/new">Create Post</a>
        </div>
        <section class="posts">
            <% if (posts && posts.length) { %>
                <% for (let post of posts) { %>
                    <article class="post">
                        <header class="post-head">
                            <p class="post-author"><%= post.username %></p>
                            <h2 class="post-title"><%= post.title %></h2>
                        </header>
                        <p class="post-body"><%= post.content %></p>
                        <% if (post.id) { %>
                            <div class="detail-actions">
                                <a class="btn btn-ghost" href="/posts/<%= post.id %>">See in Detail</a>
                                <form class="inline-form" action="/posts/<%= post.id %>?_method=DELETE" method="POST">
                                    <button class="btn" type="submit">Delete</button>
                                </form>
                            </div>
                        <% } %>
                    </article>
                <% } %>
            <% } else { %>
                <p class="empty">No posts yet.</p>
            <% } %>
        </section>
    </main>
</body>
</html>

